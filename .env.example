# Side A debater — Claude Opus 4.8
ANTHROPIC_API_KEY=
# Optional override (defaults to claude-opus-4-8)
# ANTHROPIC_MODEL=claude-opus-4-8

# Side B debater — GPT-5.5 Thinking
OPENAI_API_KEY=
# IMPORTANT: confirm the exact model id for your account and set it here.
# OPENAI_MODEL=gpt-5.5-thinking

# Neutral moderator — Google Gemini
GOOGLE_API_KEY=
# GOOGLE_MODEL=gemini-2.5-pro

# Persistence / shareable links (Vercel KV) — auto-filled when you add a KV
# store to the project on Vercel. Without these, debates are kept in memory.
KV_REST_API_URL=
KV_REST_API_TOKEN=
